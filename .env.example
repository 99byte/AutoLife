# =============================================================================
# AutoLife 环境变量配置示例
# =============================================================================
# 使用说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 填写你的实际配置值
# 3. .env 文件不会被提交到 Git（已在 .gitignore 中）
# =============================================================================

# -----------------------------------------------------------------------------
# 智谱 AI API 配置（必需）
# -----------------------------------------------------------------------------
# 获取地址：https://open.bigmodel.cn/
# 注册账号后在"API密钥"页面创建密钥
ZHIPUAI_API_KEY=your_zhipuai_api_key_here

# -----------------------------------------------------------------------------
# AutoGLM 模型服务配置
# -----------------------------------------------------------------------------

# 使用智谱官方 API 服务（推荐）
AUTOGLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
AUTOGLM_MODEL=glm-4-flash
AUTOGLM_API_KEY=${ZHIPUAI_API_KEY}

# 如果使用本地部署的模型服务，请改为：
# AUTOGLM_BASE_URL=http://localhost:8000/v1
# AUTOGLM_MODEL=autoglm-phone-9b
# AUTOGLM_API_KEY=EMPTY

# -----------------------------------------------------------------------------
# 设备配置（可选）
# -----------------------------------------------------------------------------

# ADB 设备 ID（如果有多个设备连接时需要指定）
# 查看设备列表：adb devices
# PHONE_AGENT_DEVICE_ID=your_device_id

# 设备类型（android 或 harmonyos）
# PHONE_AGENT_DEVICE_TYPE=android

# -----------------------------------------------------------------------------
# 高级配置（可选）
# -----------------------------------------------------------------------------

# 日志级别（DEBUG, INFO, WARNING, ERROR）
# LOG_LEVEL=INFO

# 最大执行步骤数
# MAX_STEPS=100

# 语言设置（cn 或 en）
# LANG=cn
